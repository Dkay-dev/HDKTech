// File này đã bị loại bỏ khi hệ phân quyền custom được hợp nhất vào
// ASP.NET Identity (permission = RoleClaim "permission:Module.Action").
namespace HDKTech.Models
{
    // Intentionally empty — permissions now live in AspNetRoleClaims.
}
